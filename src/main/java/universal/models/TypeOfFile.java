package universal.models;

/**
 * Enum {@code TypeOfFile} reprezentuje typy plików
 * rozróżniane przez system synchronizacji.
 *
 * <ul>
 *   <li>{@link #DIR} — katalog (folder).</li>
 *   <li>{@link #FILE} — zwykły plik.</li>
 * </ul>
 */
public enum TypeOfFile {
    DIR,
    FILE,
}
